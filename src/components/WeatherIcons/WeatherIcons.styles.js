import styled from 'styled-components'

export const WeatherIcon = styled.div`
  position: relative;
  width: ${props => props.size};
  aspect-ratio: 1 / 1;
  max-width: 33rem;
  overflow: hidden;
  align-self: center;
`